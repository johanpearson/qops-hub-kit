name: Publish Package

# Trigger on version tags (v*.*.*)
trigger:
  tags:
    include:
      - v*.*.*

# No manual trigger
pr: none

pool:
  vmImage: 'ubuntu-latest'

variables:
  nodeVersion: '18.x'
  # IMPORTANT: Update this to match your Azure Artifacts feed name
  artifactFeedName: 'qops-feed'

jobs:
  - job: PublishPackage
    displayName: 'Build and Publish'
    steps:
      # Setup Node.js
      - task: NodeTool@0
        displayName: 'Setup Node.js $(nodeVersion)'
        inputs:
          versionSpec: '$(nodeVersion)'

      # Install dependencies
      - task: Npm@1
        displayName: 'Install dependencies'
        inputs:
          command: 'ci'

      # Extract version from git tag
      - bash: |
          TAG_VERSION=$(git describe --tags --exact-match)
          PACKAGE_VERSION=${TAG_VERSION#v}
          echo "Git tag: $TAG_VERSION"
          echo "Package version: $PACKAGE_VERSION"
          echo "##vso[task.setvariable variable=PackageVersion]$PACKAGE_VERSION"
        displayName: 'Extract Version from Tag'

      # Update package.json version to match the tag
      - bash: |
          npm version $(PackageVersion) --no-git-tag-version --allow-same-version
        displayName: 'Update package.json Version'

      # Run quality checks
      - task: Npm@1
        displayName: 'Run Linting'
        inputs:
          command: 'custom'
          customCommand: 'run lint'

      - task: Npm@1
        displayName: 'Build Package'
        inputs:
          command: 'custom'
          customCommand: 'run build'

      - task: Npm@1
        displayName: 'Run Tests'
        inputs:
          command: 'custom'
          customCommand: 'test'

      # Publish to Azure Artifacts
      - task: Npm@1
        displayName: 'Publish to Azure Artifacts'
        inputs:
          command: 'publish'
          publishRegistry: 'useFeed'
          publishFeed: '$(artifactFeedName)'

      # Success message
      - bash: |
          echo "✅ Successfully published version $(PackageVersion) to Azure Artifacts feed: $(artifactFeedName)"
          echo "📦 Package: @qops/hub-kit@$(PackageVersion)"
          echo "🏷️  Git tag: v$(PackageVersion)"
        displayName: 'Publish Summary'

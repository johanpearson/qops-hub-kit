{
  "name": "example-api",
  "version": "1.0.0",
  "description": "Example API using @qops/hub-kit",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "prestart": "npm run build",
    "start": "func start",
    "test:integration": "npm run build && node dist/test-integration.js"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@qops/hub-kit": "file:..",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "azure-functions-core-tools": "^4.0.5611",
    "typescript": "^5.3.0"
  }
}
